/** Max options shown in searchable dropdowns (Select2-style). */
export const SEARCH_SELECT_LIMIT = 10;

export const SEARCH_DEBOUNCE_MS = 300;

export interface SearchSelectOption {
  value: string;
  label: string;
}
